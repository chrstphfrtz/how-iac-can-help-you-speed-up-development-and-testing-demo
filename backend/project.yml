parameters: {}
environment: {}
packages:
    - name: functions
      shared: false
      environment: {}
      parameters: {}
      annotations: {}
      functions:
        - name: todos
          binary: false
          main: ""
          web: true
          parameters: {}
          environment:
            DB_USER: "${DB_USER}"
            DB_PASSWORD: "${DB_PASSWORD}"
            DB_HOST: "${DB_HOST}"
            DB_NAME: "${DB_NAME}"
            DB_PORT: "${DB_PORT}"
          annotations: {}
          limits: {}
